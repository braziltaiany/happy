-arquivo server.ts
    Rotas - REQ/RES
    rota = conjunto
    recurso = usuário
    Metodos HTTP = GET(Buscar inforamção),POST(criando informação),PUT(editando informação),DELETE(deletar informação)
Parâmetros 
    Query params:http://localhost:3333/users?search=nomeuser
    Route params:http://localhost:3333/users/1 (identicar um recurso - exemplo id = 1)
    body:http://localhost:3333/users (identicar um recurso)

-banco de dados
    Driver nativo, query builder,ORM

    -migrations

TERMINAL
    Pacotes
yarn init -y
yarn add express
yarn add @types/express -D
yarn add typescript -D
yarn tsc --init
yarn add ts-node-dev -D 
yarn dev //executa o projeto
yarn add typeorm sqlite3
yarn typeorm
yarn typeorm migration:create -n create_orphanages
yarn typeorm migration:run
yarn typeorm migration:revert //exclui tabela
yarn typeorm migration:run
yarn dev
yarn typeorm migration:create -n create_images
yarn typeorm migration:run
yarn add multer
yarn add @types/multer -D
yarn add express-async-errors //captura de erros
yarn add yup
yarn add @types/yup -D
yarn add cors //funcionamento em qualquer porta
yarn add @types/cors -D

exemplo:
{
  "id": 6,
  "name": "Lar amigos felizes",
  "latitude": -22.816734,
  "longitude": -43.3648317,
  "about": "Sobre o abrigo",
  "instructions": "Venha nos visitar",
  "opening_hours": "Das 8h até 18h",
  "open_on_weekends": 1
}

